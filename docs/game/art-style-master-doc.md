# 《溪谷新芽》美术风格 Master Doc

> 版本：1.2 · 状态：角色、作物、地块、建筑、道具、特效、UI 与高清展示层生成的唯一视觉及技术母规范 · Product Owner：用户
>
> 本文件、[建筑美术风格生成 Skill](../../skills/building-art-style-generation/SKILL.md) 与[建筑规范模板](building-art-style-spec-template.md)构成建筑美术 prompt 的唯一允许来源。三者之外的参考资料只能帮助理解制作流程，不得向 prompt 注入新的风格、颜色、造型或构图规则。

## 1. 视觉目标与原创边界

### 1.1 一句话视觉语言

温暖的中世纪溪谷工艺：低分辨率像素以清晰的块面、柔和的暖光、湿润的木石和克制的青铜细节，描绘一个由水渠、梯田、木构与社区共同维护的宁静生活世界。

### 1.2 玩家应当感受到

- **温暖**：木蜜色、麦穗色、铜橙色是光与生活的来源；暗部偏综合色，不用生硬的纯黑。
- **清晰**：远看先读轮廓，中看读功能，近看读材质；可交互、可建造、可损坏和可升级的部分都有明确反馈。
- **低分辨率**：像素是设计语言，不是最后一步的缩小滤镜；每个像素都服务于轮廓、材质或状态。
- **明确反馈**：建筑状态、入口、屋顶层级、可通行边界、升级变化和受损部件，必须靠形状、明度、动画或标记共同表达。
- **中世纪典雅**：采用拱、梁、瓦、石基、木窗、铜件、织物旗帜等历史感构件的抽象组合；不复刻现实建筑、影视场景或任何既有游戏的具体建筑。

### 1.3 参考与禁止模仿

可以参考成熟农场生活模拟类型带来的体验：亲切的低分辨率像素、暖色乡村环境、功能一眼可辨的建筑、角色轮廓鲜明、交互反馈直接。不得参考或复制任何特定作品的精确像素尺寸、提取调色板、建筑立面、屋顶/门窗组合、角色发型服装、UI 排版、地图构图、动画帧、图标或可识别的画师风格。特别是不得以《星露谷物语》的具体像素、颜色值、建筑或角色设计作为生成指令；本文件中的规则是独立重建的原创系统。

**原创性红线：** 禁止描摹、临摹、图生图改色、拼接现成 sprite、复用角色/建筑/地图轮廓，或生成“看起来像某具体游戏”的变体。若 silhouette、配色关系或装饰组合出现可识别近似，退回重设计。

### 1.4 双层美术合同

本项目从 2026-08-21 起采用两条并存、不可混用的表现层：

1. **地图运行层（Pixel Runtime）**：角色、地块、作物、建筑、道具、特效和 UI 继续执行本文件既有原生像素规格，使用 nearest 与整数缩放。该层负责移动、碰撞、入口、遮挡、状态和实时反馈。
2. **高清展示层（HD Presentation）**：对话立绘、人物信息、建筑介绍、商店面板和场景切换使用独立 2048×2048 RGBA 插画。该层只负责清晰展示，不承载碰撞、地图拓扑、稳定 ID 推断或存档状态。

高清展示层以 N-009 完整全身立绘的清晰度和绘制质感为项目内部基准：清楚的轮廓线、细腻但克制的布料/木石/金属纹理、柔和暖光、自然冷暖过渡和 100% 查看时可辨的近景细节。禁止把低分辨率像素图放大后冒充高清展示图，也禁止用锐化、插值或 2048 画布尺寸替代原生细节生产。

两层必须共享同一主体身份、silhouette、功能锚点、主色关系和原创识别件，但允许采用不同的渲染密度：运行层优先远景读形，展示层补充近景材质。高清展示层不得反向覆盖或替换已批准的原生像素运行资产。

## 2. 像素规格与镜头

| 项目 | 锁定规则 |
|---|---|
| 运行视角 | 俯视与轻微三分之四结合；建筑正面/侧面必须同时露出可读的底座、入口和屋顶层级 |
| 目标内部画布 | 16:9 逻辑分辨率；所有缩放为整数倍，保持像素边缘锐利 |
| 建筑基准网格 | 24×24 px tile；建筑 footprint、门、路径、地基优先对齐 24 px 网格 |
| 建筑生成母稿 | 96×96、144×144 或 192×192 px 的整数倍母稿；最终运行尺寸必须可无损落到 24 px 网格 |
| 角色关系 | 角色基准 32×48 px；建筑门、窗、台阶要以该尺度检查可进入性和比例 |
| 线条 | 1 px 主轮廓；重要遮挡可用 2 px 局部加厚；禁止全局黑色粗描边 |
| 缩放/导入 | 最近邻过滤、整数缩放、无抗锯齿、无自动压缩；透明边界干净 |
| 细节密度 | 轮廓优先，功能细节次之，装饰最后；小型建筑避免超过 3 个高对比焦点 |

### 2.1 高清展示层规格

| 项目 | 锁定规则 |
|---|---|
| 画布与格式 | 2048×2048 RGBA8；真实透明背景；透明像素 RGB 清零 |
| 构图 | 单一主体居中且完整；头脚或建筑屋顶/地基/入口/附属功能件均不得裁切；四边保留安全透明边距 |
| 镜头 | 人物使用完整全身展示；建筑使用轻微俯视三分之四视角，并保持同一批次镜头高度 |
| 渲染语言 | 高清绘本式游戏插画；清楚线稿、细腻材质、柔和暖光与冷青平衡；不是像素图、3D 渲染或照片写实 |
| 缩放/导入 | 展示层保持原始比例，可使用线性缩放；禁止 nearest 放大像素块，禁止非等比拉伸 |
| 色彩 | 沿用第 3 节色票的色相和功能关系，可为绘本明暗自然混合邻近色；不得引入新的高饱和主色身份 |
| 禁止 | 场景背景、人物陪衬、文字、Logo、水印、签名、投影底板、第二主体、棋盘格烘焙 |
| 清晰度验收 | 在 100% 查看时与 N-009 立绘具有相近的线条清晰度、材质层次与局部细节密度；只通过尺寸/Alpha 检查不算清晰度通过 |

## 3. 锁定调色板

以下是本项目独立的起始色票，不是任何外部作品的取色结果。生成 prompt 只能使用色票名称及其十六进制值；新颜色须先写入本表并经内容负责人批准。

| 色票名 | HEX | 主要用途 | 明度角色 |
|---|---|---|---|
| 深溪墨 | `#263638` | 最深结构缝、远处遮挡、文字阴影 | 低 |
| 湿石褐 | `#55484A` | 石基、泥土暗面、木石接缝 | 低中 |
| 栗木影 | `#765247` | 木构暗面、屋檐下、角色发色 | 中低 |
| 苔叶绿 | `#788653` | 草、叶、苔、生态标记 | 中 |
| 雾蓝 | `#7897A0` | 水面、阴天、冷反光、织物 | 中 |
| 河岸青 | `#4F7070` | 水渠边、铜件冷反光、状态辅助色 | 中低 |
| 麦秆金 | `#B58A52` | 稻草、木蜜高光、可收获提示 | 中高 |
| 木蜜 | `#C58A59` | 木材主面、暖光、家具 | 中高 |
| 铜橙 | `#C66E3D` | 青铜水工件、重点交互、升级强调 | 高 |
| 乳白纸 | `#F0D9AD` | 纸张、窗光、UI 浅底、可读高光 | 高 |

### 调色与明度规则

1. 单个建筑常规使用 5–8 个色票：深溪墨/湿石褐作为暗部，木蜜/麦秆金作为暖面，雾蓝/河岸青作为冷平衡，铜橙只做焦点。
2. 暖冷关系优先于纯粹明暗：夜间不把所有颜色压成黑蓝；雨天保留木蜜色，只降低饱和度并增加雾蓝反光。
3. 可交互对象和入口至少比相邻背景高一个明度层级；危险/阻挡状态不得只靠红色，必须叠加形状、纹理或动画。
4. 禁用纯黑大面积填充、荧光色、金属渐变、照片纹理、软阴影、半透明模糊和连续抗锯齿边缘。

## 4. 材质语言

- **木**：大块平面 + 2–3 条沿结构方向的短像素纹；暖面用木蜜，接缝用栗木影，不画写实木纹。
- **石**：不规则但可归纳的块面；湿面增加一小段雾蓝高光，干面保留湿石褐；石基比墙体更深、更重。
- **屋顶**：层叠短条或菱形瓦片，瓦片方向必须服从屋脊；只在屋檐和破损处使用高对比。
- **金属/青铜**：铜橙小面积高光 + 河岸青冷面 + 深溪墨接缝；避免镜面渐变。
- **织物/旗帜**：麦秆金、雾蓝或苔叶绿的大色块；用 1–2 个折线像素表达褶皱，不能喧宾夺主。
- **水与玻璃**：雾蓝基底、乳白纸短高光、河岸青阴影；用 2–4 个有节奏的块状反射表达流动。

## 5. 角色、建筑、UI 与动画统一规则

### 5.1 角色

角色以 32×48 px 读形为基准：头、躯干、手部工具、鞋和职业色块必须分离；职业身份由工具、姿势和 1 个主色块表达，不依赖脸部细节。角色不可使用现成农场游戏的发型、服装、脸型或配色组合。建筑门口须留出至少 1 个角色宽度的可读入口，并在遮挡时显示脚部/交互标记。

### 5.2 建筑

建筑必须先有独立 silhouette，再有材质。核心识别来自“水工功能 + 中世纪木石结构 + 溪谷环境”三者组合，例如分层水轮、窄高谷仓、带导水檐的工坊；不得用通用方盒加屋顶直接套皮。每栋建筑至少定义：功能锚点、入口、屋顶层级、基础、一个原创识别件和一种状态变化。

### 5.3 UI

UI 使用乳白纸/湿石褐底，深溪墨文字，铜橙或苔叶绿做重点。面板采用纸签、木牌、铜扣的抽象语汇，但不可复刻任何既有作品的布局、图标或字体。状态必须同时有文字/图标/形状或动画表达，不能只用颜色。

### 5.4 动画

动画保持 2–6 帧的低成本节奏：门开合、旗帜轻摆、水流循环、烟雾上升、升级部件闪一下。反馈顺序是“动作发生 → 建筑状态变化 → 短暂强调 → 回到稳定态”；禁止持续闪烁、快速频闪和难以阅读的装饰运动。动画帧必须保持同一 silhouette、基线、调色板和像素密度。

## 6. 建筑设计硬规则

1. 每栋建筑提交三张独立 silhouette：`A 功能优先`、`B 轮廓优先`、`C 叙事/环境优先`，并由内容负责人选择一张进入生产。
2. silhouette 必须在 1×、2× 放大、灰度和纯色缩略图下仍可区分；不能靠纹理或文字才认出功能。
3. 建筑分为 `base`、`structure`、`roof`、`detail`、`interaction`、`state_fx` 六层；层之间不得把可变状态烘焙进不可变底图。
4. 建筑的运行时碰撞、入口、遮挡和交互点必须可追踪到规范模板中的网格与 ID，不由图像猜测。
5. 任何升级至少改变一个轮廓层级或功能锚点，并保留原建筑身份；只换颜色不算升级。
6. 最终资产须通过像素检查、最近邻导入检查、运行时截图检查和原创性检查，缺一不可。

## 7. Prompt 约束短版

后续建筑 prompt 必须包含：`原创溪谷中世纪木石建筑`、`24×24 像素网格`、`暖色但有雾蓝冷平衡`、`深溪墨而非纯黑粗边`、`清晰功能入口与可读 silhouette`、`六层生产结构`、`最近邻整数缩放`、`不近似任何既有游戏或现实地标`。必须从[建筑规范模板](building-art-style-spec-template.md)填充建筑的唯一变量；不得添加外部风格名、外部色板或外部作品名。

## 8. 变更与优先级

冲突时按以下优先级执行：原创性红线 > 可读性/反馈 > 像素规格与导入安全 > 调色板与材质 > 装饰细节。任何修改先更新本 Master Doc，再更新 Skill 与模板；未同步前，不得用于生成新资产。

## 9. 独立元素资产技术表

本节把已确认的场景锚点拆成可单独生成、审核、替换和导入的资产。后续每次只生成一个元素或一个严格同类的状态组；禁止把角色、背景、文字、阴影底板和无关道具烘焙进同一张运行时 PNG。

### 9.1 全局生产合同

| 项目 | 概念源图 | 运行时资产 |
|---|---|---|
| 用途 | 评审造型、比例、材质和配色；不可直接入游戏 | SpriteKit 实际加载与显示 |
| 建议画布 | 单元素 512×512 或 1024×1024；建筑可用 1024×1024 | 严格使用下表原生像素尺寸 |
| 背景 | 透明优先；不能透明时用纯色并在交付前去背 | 独立元素必须 RGBA 透明；铺满型地块可为不透明 |
| 构图 | 单个对象居中、完整、不裁切；无场景、无文字、无投影底板 | 使用固定锚点和透明留白；不得自动裁切改变原点 |
| 像素方式 | 可用于造型提案，但不能依赖最后一步“缩小+像素化” | 在目标原生尺寸逐像素重绘/清理；无抗锯齿、无渐变、无半透明软边 |
| 色彩 | 只能使用本文件第 3 节锁定色票 | RGBA8；色票不得漂移；透明像素 RGB 清理为 0 |
| 缩放 | 仅用于审阅 | `SKTexture.filteringMode = .nearest`；只允许整数倍 |
| 命名 | `concept_<category>_<slug>_v##.png` | `<category>_<slug>[_<state>][_f##].png`，全小写 snake_case |
| 版本策略 | 新版本新增文件，不覆盖已批准概念 | 同名替换前保存审核记录；稳定运行时 ID 不随美术改名 |
| 禁止 | 多元素拼贴、外部作品名、外部色板、画师名、Logo、水印 | JPG、线性过滤、非整数缩放、未声明图层、从像素颜色推断碰撞 |

**当前运行时硬规格：** 地块 `24×24 px`、作物 `32×32 px`、角色 `32×48 px`。该规格与 `PixelAssetStore.swift` 及现有 PNG 一致；早期 N-003 文档中“作物 24×24”的表述由本节更正为 `32×32`。

### 9.2 角色独立资产

#### 角色通用输出

| 阶段 | 输出 | 规格 | 锚点/方向 | 验收重点 |
|---|---|---|---|---|
| C0 造型确认 | 每人一张全身概念图 | 512×512 或 1024×1024，透明 | 正面站姿、脚底居中 | silhouette、职业色块、道具、原创性 |
| R0 静态接入 | 每人一张 idle 正面 PNG | 32×48 | bottom-center；面向下 | 1× 可辨、无裁切、与门/地块比例正确 |
| R1 基础活动 | 每人一张行走 spritesheet | 128×192；每帧 32×48 | 4 列×4 行；行序 `down,left,right,up`；每行 4 帧 | 基线不跳、轮廓不闪、nearest 锐利 |
| R2 玩家动作 | 玩家每种工具一张 spritesheet | 128×192；每帧 32×48 | 同 R1 | 锄地、浇水、收获动作与目标格反馈一致 |

| 优先级 | 角色 ID / 名称 | 运行时文件基名 | 主色与识别件 | 首轮必须交付 |
|---|---|---|---|---|
| P0 | `player` / 新芽农人 | `char_player` | 木蜜衣、苔叶绿边、锄头 | C0 + R0 |
| P0 | `brookseed.npc.water_apprentice` / 水工学徒 | `char_water_apprentice` | 雾蓝工装、青铜水闸扳手、小木铲 | C0 + R0；现管线同名替换 |
| P0 | `brookseed.npc.seed_steward` / 种源管理员 | `char_seed_steward` | 苔叶绿罩衫、种子布袋、种碟 | C0 + R0；现管线同名替换 |
| P0 | `brookseed.npc.creek_warden` / 溪岸巡护员 | `char_creek_warden` | 湿石褐外套、巡护帽、渔网木桶 | C0 + R0；现管线同名替换 |
| P1 | `brookseed.npc.neighbor_hearsay` / 涧麦婶 | `char_neighbor_hearsay` | 麦秆金围裙、菜篮、盘髻 | C0 + R0 |
| P1 | `brookseed.npc.neighbor_storyteller` / 石灯 | `char_neighbor_storyteller` | 铜橙外袍、小石灯、夸张手势 | C0 + R0 |
| P1 | `brookseed.npc.neighbor_evidence` / 青砚 | `char_neighbor_evidence` | 河岸青长衫、卷纸、严谨姿态 | C0 + R0 |
| P1 | `brookseed.npc.neighbor_consensus` / 絮宁 | `char_neighbor_consensus` | 乳白纸浅衫、浅褐垂发、收敛姿态 | C0 + R0 |

角色眼睛、嘴和手指不是首要识别信息；在 32×48 中优先保住头发外轮廓、躯干色块、工具和鞋部接地。不同人物不能只靠换色区分。

### 9.3 作物独立资产

#### 作物通用输出

| 阶段 | 输出 | 规格 | 命名 | 说明 |
|---|---|---|---|---|
| C0 物种确认 | 每种作物一张成熟态概念图 | 512×512，透明 | `concept_crop_<slug>_v##.png` | 单株、无土地底板、无花盆、无文字 |
| R0 成熟态接入 | 每种作物一张成熟 PNG | 32×32 | `crop_<slug>.png` | 保持当前 `PixelAssetStore` 同名兼容 |
| R1 生长组 | 每阶段一张 PNG | 32×32 | `crop_<slug>_stage_0.png` 至 `_stage_3.png` | `0 种子/土芽、1 幼芽、2 成长、3 成熟`；成熟态与 R0 造型一致 |

| 优先级 | 作物 | slug / 文件基名 | 成熟态识别规则 | 首轮必须交付 |
|---|---|---|---|---|
| P0 | 雾萝卜 | `mist_radish` / `crop_mist_radish.png` | 雾蓝根部露土、苔叶绿短叶簇、低矮圆轮廓 | C0 + R0 |
| P0 | 溪叶菜 | `stream_leaf` / `crop_stream_leaf.png` | 细长叶向同一溪流方向弯垂、根部湿石褐 | C0 + R0 |
| P0 | 琥珀豆 | `amber_bean` / `crop_amber_bean.png` | 小竹架形成高轮廓、麦秆金/木蜜豆荚 | C0 + R0 |
| P0 | 铃花莓 | `bell_berry` / `crop_bell_berry.png` | 低矮灌木、铃形铜橙果、雾蓝短高光 | C0 + R0 |
| P0 | 蜜穗瓜 | `honey_melon` / `crop_honey_melon.png` | 木蜜瓜体卧地、浅纹和卷须形成横向轮廓 | C0 + R0 |

成熟、未成熟与枯萎不能只靠颜色区分：同时改变高度、叶片展开程度、果实轮廓或垂落姿态。浇水状态由地块和短时水光反馈表达，不额外复制一套染蓝作物。

### 9.4 地块与水体独立资产

所有地块为 `24×24 px`，锚点 center，默认不透明；边缘类素材允许透明。无缝地块必须通过 `3×3` 平铺检查，且不得在单 tile 中放置明显中心花纹。

| 优先级 | 元素/状态 | 文件名 | 数量 | 透明 | 技术要求 |
|---|---|---|---:|---|---|
| P0 | 草地基础 | `tile_grass.png` | 1 | 否 | 当前管线同名；低频苔点，不出现十字接缝 |
| P0 | 翻耕干土 | `tile_tilled.png` | 1 | 否 | 当前管线同名；沟纹方向统一 |
| P0 | 水岸示意 | `tile_water_edge.png` | 1 | 是 | 当前管线同名；作为试点回退，不承担完整自动拼接 |
| P1 | 草地变化 | `tile_grass_a/b/c.png` | 3 | 否 | 变化只在低对比苔点，随机铺设不闪烁 |
| P1 | 已浇水土 | `tile_tilled_watered.png` | 1 | 否 | 比干土更深，并增加形状明确的短水光 |
| P1 | 水面循环 | `tile_water_f00.png` 至 `_f03.png` | 4 | 否 | 4 帧循环；波纹位置连续，无全屏闪动 |
| P1 | 水岸直边 | `tile_water_edge_n/e/s/w.png` | 4 | 是 | 边缘与 center 水面像素相接 |
| P1 | 水岸外角 | `tile_water_corner_ne/se/sw/nw.png` | 4 | 是 | 透明角落干净，无一像素裂缝 |
| P1 | 湿石路径 | `tile_stone_path.png` | 1 | 是 | 24×24，能与草地叠加；不可与石基混淆 |
| P1 | 水渠接片 | `tile_canal_ns.png`、`tile_canal_ew.png` | 2 | 是 | 流向可由形状读出，不只靠水色 |

### 9.5 建筑独立资产

建筑严格执行本文件第 6 节和建筑生成 Skill：先三剪影，再拆为 `base/structure/roof/detail/interaction/state_fx` 六层。下表尺寸是透明母画布，不等于碰撞范围；footprint 和入口必须另行填写建筑规范模板。

| 优先级 | 建筑/稳定 ID | 参考母画布 | 建议 footprint | 原创识别件 | 首轮状态 |
|---|---|---|---|---|---|
| P0 | 木构农舍 / `brookseed.landmark.farm_house` | 144×144 | 5×4 tile | 双层暖木坡顶、湿石基、清晰中央入口 | `default`；六层 |
| P0 | 铜闸水工坊 / `brookseed.landmark.farm_sluice` | 192×168 | 5×4 tile | 非地标式木石坡顶、拱门、紧凑侧水轮、青铜闸管 | `default/active`；六层 |
| P1 | 种源棚 / `brookseed.landmark.market_seed_shed` | 96×96 | 3×2 tile | 种匣窗、短檐、苔叶绿织物牌 | `default/open`；六层 |
| P1 | 巡护亭 / `brookseed.landmark.market_warden_post` | 96×96 | 3×2 tile | 湿石矮基、挂网架、雾蓝观察窗 | `default/open`；六层 |
| P1 | 苔石埠头 / `brookseed.landmark.market_wharf` | 144×96 | 5×2 tile | 层叠湿石、木系缆桩、可读水边入口 | `default/wet`；按六层语义拆分 |

每栋建筑的运行时文件采用 `building_<slug>_<layer>.png`；状态变化采用 `building_<slug>_<layer>_<state>.png`。屋顶遮挡层必须可独立隐藏，不得和碰撞或入口数据烘焙在图片中。

### 9.6 放置物、资源与工具

| 优先级 | 元素/稳定 ID | 运行时尺寸 | 文件名 | 状态/方向 |
|---|---|---:|---|---|
| P1 | 木箱 / `brookseed.placed.wooden_crate` | 24×24 | `prop_wooden_crate.png` | `closed/open` 后续可拆两张 |
| P1 | 小径 / `brookseed.placed.stone_path` | 24×24 | `prop_stone_path.png` | 单态；与 `tile_stone_path` 共用视觉语言但不同 ID |
| P1 | 堆肥架 / `brookseed.placed.compost_rack` | 48×48 | `prop_compost_rack.png` | 2×1 footprint；空/满后续拆分 |
| P1 | 水渠接片 / `brookseed.placed.canal_segment` | 24×24 | `prop_canal_segment_ns/ew.png` | 南北/东西两方向 |
| P1 | 雨水桶 / `brookseed.placed.rain_barrel` | 24×32 | `prop_rain_barrel.png` | 空/满用水面形状叠层表达 |
| P1 | 溪木、苔石、芦纤维采集点 | 24×24 | `gather_creek_wood/moss_stone/reed_fiber.png` | `available/depleted` 两态 |
| P1 | 锄头、浇水壶、收获手套 | 24×24 | `tool_hoe/watering_can/harvest_glove.png` | UI 图标；透明背景 |

### 9.7 特效与 UI 元素

| 优先级 | 元素 | 每帧尺寸 | 帧数 | 文件/图集规则 | 无障碍要求 |
|---|---:|---:|---:|---|---|
| P1 | 目标格有效/无效 | 24×24 | 2+2 | `fx_target_valid_f##` / `fx_target_invalid_f##` | 形状分别为完整角框/断裂角框，不能只用颜色 |
| P1 | 浇水飞溅 | 32×32 | 4 | `fx_water_splash_f00..03.png` | 短时、无快速频闪 |
| P1 | 收获反馈 | 32×32 | 4 | `fx_harvest_f00..03.png` | 果实轮廓+上浮方向明确 |
| P1 | 水流激活 | 24×24 | 4 | `fx_canal_flow_f00..03.png` | 水光位置连续，静帧也能读出流向 |
| P1 | 交互提示底框 | 24×24 | 1 | `ui_interact_badge.png` | 图标之外保留文字接入空间 |
| P1 | 背包槽/选中槽 | 24×24 | 1+1 | `ui_slot.png` / `ui_slot_selected.png` | 选中态增加边框形状和明度，不只换色 |
| P1 | 时间、天气、体力、货币图标 | 24×24 | 各 1 | `ui_time/weather/stamina/currency.png` | 1× 可辨；无文字烘焙 |

### 9.8 第一轮实际生成清单与顺序

第一轮只锁风格和尺度，不立即制作全部动画：

1. `char_player`、`char_water_apprentice`、`char_seed_steward`、`char_creek_warden`：4 张独立 C0 概念图，确认后各出 32×48 R0。
2. 五种作物：5 张独立成熟态 C0，确认后各出 32×32 R0，同名替换现有兜底 PNG。
3. `tile_grass`、`tile_tilled`、`tile_water_edge`：3 张 24×24 R0，用 3×3 平铺图共同验收。
4. 木构农舍、铜闸水工坊：分别先做三剪影；选定后再制作六层资产。
5. 第一轮风格与尺寸通过后，才进入四方向行走、生长阶段、完整水岸、放置物、特效和 UI。

第一轮预计交付：`4` 张角色概念 + `4` 张角色静态运行图 + `5` 张作物概念 + `5` 张作物成熟运行图 + `3` 张地块 + `6` 张建筑剪影 + `12` 张建筑分层 PNG，共 `39` 个文件；评审图、生成记录和原创性报告另计。

### 9.9 单元素验收记录

每个元素必须记录：`stable_id`、概念文件、运行时文件、原生尺寸、透明度、锚点、状态/方向、色票数量、生成 prompt 版本、原创性结论、1×/4×预览、nearest 结果、门禁状态。缺少任一项时不得进入批量生成或运行时替换。
