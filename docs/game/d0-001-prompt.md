# D0-001 Cursor 开工 Prompt（粘贴给 Cursor / Codex）

> 本文件是 `docs/game/d0-001-brief.md` 的执行版。Cursor 是唯一实现负责人；完成后经 🧱 tech 交付门禁与 🛡️ quality 独立验收，任何人不得自行 APPROVED。

## 1. 任务一句话

在现有 Xcode + Swift + SpriteKit 工程（CreekSprout）上集成验证 VS-1 完整垂直切片：走通 PRD 第 14 节全部 7 步场景（45–60 分钟自然路径），打磨关键占位表现（HUD/反馈/教程引导/核实入口 UX 澄清），产出问题清单与试玩反馈文档。不做 M4 内容。

## 2. 当前状态（独立核实过的事实，证据路径见括号）

- 工程在 `macos/CreekSprout/CreekSprout.xcodeproj`，scheme `CreekSprout`，macOS arm64 / Xcode 26.6 / macOS 26.5.2。
- M1→M3 已全部验收：移动/农耕/背包/出售 174 溪票/任务/采集/水渠 12+8=20/两张地图/七名角色/社区事件五行动状态机/风评/信任子点/schema v6 均已实现（台账 `docs/game/project-ledger.md`）。
- 当前测试基线 **180/180 passed、0 failed、0 skipped、0 警告**，权威计数来自 `artifacts/integration/m3-003-xcode/xcode-evidence/TestResults-M3-003.xcresult`（M3-002 基线 127/127，M3-003 新增 53）。
- 社区事件规格：第 2 天 `offered`、风评 50、截止第 3 天 23:30；行动 `relay_unverified`(−8)/`defer_judgment`(0)/`start_verification`(0)/`verify_with_c`(0)/`correct_d`(+8，水脉+2 或信任+300 二选一)；边界 `min_standing=41`，39→31 暂停、39→47 恢复（PRD 6.9、14.5/14.6）。
- 已知 UX 缺陷（R1）：HUD/任务面板文案可能让玩家误以为 **V 键一步核实**（青砚的可选恢复路径）是首次核实入口；首次核实必须走 `investigating` 状态下的「向青砚核实」行动。D0-001 必须澄清此文案。
- 证据目录约定：`artifacts/integration/d0-001-xcode/`（不存在则创建）。

## 3. 实现边界

### 必须做（In scope）

1. **端到端集成验证**：PRD 14 节 7 步场景逐一在真实构建走通并留证据（见第 6 节）。
2. **关键占位表现打磨（仅表现层）**：HUD 排版与信息层级（风评/事件状态/截止/信任/任务/水脉）、反馈文案（成功/失败/暂停/恢复，不依赖颜色）、教程与任务引导清晰度、对话与锁提示、R1 核实入口文案澄清。
3. **运行时缺陷修复**：集成走查发现的缺陷最小改动修复；域事务语义（幂等/原子/回滚/信任数值/水脉贡献）不得改变。
4. **问题清单与试玩反馈文档**：`issues-feedback.md`，含严重度分级（阻断/高/中/低）、已知问题、非阻断打磨项、M4 建议输入。
5. **回归**：全部既有测试保持通过，新增回归测试覆盖修复点。

### 禁止做（Out of scope / 红线，Do Not）

- ❌ 不改 PRD、TDD、`docs/game/project-ledger.md`、本 brief、既有 brief 与既有证据文件。
- ❌ 不自我 `APPROVED`，不修改任务状态，不宣称验收通过。
- ❌ 不引入正式美术/精灵/角色动画/正式音频/混音（属 M4）。
- ❌ 不做性能/加载/内存优化与预算（属 M4-001）。
- ❌ 不新增玩法功能、地图、角色、任务、经济数值、赠礼/日程扩展（PRD 9.2 不含项）。
- ❌ 不改 `Domain/` 事务核心语义、`Persistence/` schema 与迁移、`ContentID.swift` 稳定 ID、`ContentValidator.swift` 校验规则（缺陷修复除外，且必须最小改动并在 handoff 声明）。
- ❌ 不删除/重写 M1–M3 证据、不重命名稳定 ID、不弱化既有测试断言。
- ❌ 不 `git commit`、不 `git reset`、不删除工作区未提交内容（M1–M3 工作树与 D0 改动共存，直接在其上增量工作）。
- ❌ 不复制任何受保护的任务、地图、对白、视觉或音频表达；所有文案原创。

## 4. 可观察验收（全部需证据）

1. clean Build/Test ≥180/180 passed、0 failed、0 skipped、0 编译警告（xcresult 权威计数，含新增测试）。
2. VS-0 回归：新游戏→收获 3 株教学雾萝卜→投入出售箱→翻土/播种/浇水→学徒交谈→手动保存→提前睡眠→174 溪票与明细（10–15 分钟内）。
3. 第 2 天雨天自动浇水；事件 `offered`、风评 50、截止第 3 天 23:30。
4. 水渠主线：保底采集→制作放置接片 +12→交付 +8→20→地图/环境音/新采集点/雨水桶配方解锁；任意社交分支后主线仍可达不回退。
5. 三夹具：转述 42（当日归零+次日 +1.50 一次）；暂缓 50 重启保持、到期无惩罚；核实纠正 58（历史 3 条、奖励一次、幂等）。
6. 边界：39→31 暂停、纠正 47 恢复；主线两分支不暂停。
7. 3 个脚本化游戏日完整走通；自然路径 45–60 分钟计时达标（边界夹具不计时）。
8. 保存→完全退出→重启→读取：待结算/结算记录/农田/库存/货币/任务/水渠/信任子点/社区行动历史/设置全一致。
9. R1 UX 澄清落地：文案明确首次核实入口是「向青砚核实」而非 V 键；HUD/反馈/引导可评审（截图可见）。
10. `issues-feedback.md` 产出；未引入 M4 内容。

## 5. 一条烟雾路径（计时主路径）

新游戏（农舍醒来）→ 收获 3 株教学雾萝卜 → 投入出售箱 → 翻土/播种/浇水 3 格 → 与水工学徒交谈 → 手动保存 → 提前睡眠 → 174 溪票结算 →（第 2 天）雨天自动浇水 → 涧麦婶处 Y/B「开始核实」→ 青砚处「向青砚核实」→ 絮宁处「纠正絮宁」（风评 58、水脉 +2）→ 保底采集 → 制作并放置水渠接片 +12 → 交付 +8 → 水脉 20、地图/音效/采集点/配方解锁 → 睡眠至第 3 天 → 保存 → 完全退出 → 重启 → 读取 → 状态一致。计时 45–60 分钟；暂不判断夹具与 39 边界夹具由测试覆盖，不计时。

## 6. 交付格式（全部放入 `artifacts/integration/d0-001-xcode/`）

1. `cursor-handoff.md`：outcome、变更文件清单（每文件一句）、验收映射表（10 条逐条 → 实现位置 + 证据）、R1 处理说明、已知问题与风险、下一负责人。
2. Build/Test 原始证据：
   ```text
   xcodebuild -project macos/CreekSprout/CreekSprout.xcodeproj -scheme CreekSprout \
     -destination 'platform=macOS,arch=arm64' \
     -derivedDataPath /tmp/creeksprout-d0-001 \
     -resultBundlePath artifacts/integration/d0-001-xcode/TestResults-D0-001.xcresult \
     clean test
   ```
   原始日志 + `test-summary.json` + 编译警告计数。若 R1 修复后测试计数 >180 属正常（新增回归测试），记录新旧计数。
3. 运行时证据：关键节点截图（教学/174 结算/雨天/事件三应对/核实纠正/水渠 20/边界暂停恢复/重启读取一致）、存档 JSON 样本、HUD 文本转储、计时记录（起止时间与分段）。
4. `issues-feedback.md`：严重度分级问题清单、已知问题、非阻断打磨项、M4 建议输入。
5. `git diff --check` 退出 0 的输出；声明未修改文件清单（PRD/TDD/台账/brief/证据）。

## 7. 回复格式（必须逐项确认，缺项视为未完成）

```
A. 范围确认：本任务 in scope 与红线逐条「确认 / 不确认」；不确认项说明原因。
B. 当前基线核验：运行前 180/180 基线是否已按第 2 节证据路径核实。
C. 交付清单：第 6 节各文件路径 + 状态（已生成/未生成）。
D. 测试结果：xcresult 权威计数（passed/failed/skipped/警告），与第 4.1 条对比。
E. 验收映射：10 条验收逐条 → 证据文件；未达标条目明确声明 REVISE 待办。
F. 风险与红线声明：声明未触碰任何红线；如有缺陷修复，列出最小改动清单。
G. 下一负责人：🛡️ quality（Hermes）独立验收。
```

完成后将本文件与 `docs/game/d0-001-brief.md` 一并交给 🧱 tech 与 🛡️ quality 门禁。
