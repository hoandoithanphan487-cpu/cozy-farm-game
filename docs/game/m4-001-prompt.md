# M4-001 Cursor 开工 Prompt（粘贴给 Cursor / Codex）

> 本文件是 `docs/game/m4-001-brief.md` 的执行版。Cursor 是唯一实现负责人；完成后经 🧱 tech 交付门禁与 🛡️ quality 独立验收，任何人不得自行 APPROVED。

## 1. 任务一句话

在现有 Xcode + Swift + SpriteKit 工程（CreekSprout）上完成 M4-001：Release 构建达到性能合同（1920×1080 10 分钟压力路线预热后帧时间 P95 ≤16.67 ms、P99 ≤25 ms）、切图加载 P95 <3 s、内存无持续增长；实现多槽存档（3 手动槽 + 1 最近自动槽，失败安全）；落地 D0 评审排期的 UX 修复（高-2/高-3/中-5/中-6/中-7）与无障碍补充。不做 M4-002 发行打包。

## 2. 当前状态（独立核实过的事实，证据路径见括号）

- 工程在 `macos/CreekSprout/CreekSprout.xcodeproj`，scheme `CreekSprout`，macOS arm64 / Xcode 26.6 / macOS 26.5.2；参考设备 M4 MacBook Air / 16 GB。
- D0 阶段全部完成：D0-001 集成切片验收（198/198）、D0-002 设置功能验收（208/208，双门禁）——设置文件/重绑定/无障碍设置（UI 缩放 100/125/150%、震动、减少闪烁、音量分类、文字速度四档）已实现，schema 已至 v7（台账 `docs/game/project-ledger.md` 行 56–57）。
- 当前测试基线 **208/208 passed、0 failed、0 skipped、0 编译警告**，权威计数来自 `artifacts/integration/d0-002-xcode/test-summary.json` + `TestResults-D0-002.xcresult`（schema_version 7）。
- 存档现状：`SaveStore.swift` 已参数化 `slotName`（默认 `slot_0`），已有 `.json`/`.backup`/`.tmp` 三件套（安全替换 + 备份基础），但对外仍是单槽入口；多槽为入口层改造，schema 如涉增量需最小迁移。
- D0 评审排期（`docs/game/d0-review.md` + `issues-feedback.md`）：高-2 HUD 可折叠/缩小（地图至少露出石阶与三名事件 NPC）；高-3 第 3 天目标行随事件状态切换（不再复述第 2 天核实教程）；中-5 水脉显示「20 已达成（含纠正 +2 → 22）」；中-6 石阶离开地图需确认或「面对石阶按空格」（防收获朝南误传送）；中-7 配方选择在切图/读档后保持。中-4 已由 D0-002 承接，不重复。
- 性能合同：NFR-001（P95 ≤16.67 ms / P99 ≤25 ms，Release，1920×1080，10 分钟压力路线预热后）、NFR-002（连续切图 10 次加载 P95 <3 s）；内存无硬性数字，本任务建 RSS 基线并记录（PRD 无阈值，以测量结果 + PO 确认为准）。
- 证据目录约定：`artifacts/integration/m4-001-xcode/`（不存在则创建；性能原始证据放 `m4-001-xcode/perf/`）。

## 3. 实现边界

### 必须做（In scope）

1. **性能测量与优化**：Release 构建（1920×1080）10 分钟压力路线（翻种浇/采集/制作/放置/交谈/事件/切图/睡眠循环）帧时间 P95/P99 基线；热点优化（HUD 每分钟整页重排为已知候选热点）；优化后达到 NFR-001。
2. **加载测量**：连续切图 10 次（农场↔溪岸集市往返），进入地图加载 P95 <3 s（NFR-002）。
3. **内存**：10 分钟压力路线 RSS 起始/峰值/结束记录，无持续增长/泄漏迹象。
4. **多槽存档（PRD 6.12/388 缺口）**：至少 3 个手动存档槽 + 1 个最近自动槽（睡眠自动保存）；暂停/设置菜单允许手动保存（与 D0-002 设置界面协调，不重复实现）；写入失败安全替换 + 备份回退不破坏最近有效存档（NFR-003）。
5. **UX 修复（D0 评审排期）**：高-2 HUD 可折叠/缩小；高-3 目标行随事件状态切换；中-5 水脉显示 22/20 新口径；中-6 石阶离开确认或「面对再按」；中-7 配方选择切图/读档后保持（同会话保活或入档）。
6. **无障碍补充**：动态字体/文本随 UI 缩放正确适配、减少半透明叠字、色觉以外的状态图标（与 D0-002 缩放/文字速度/震动/闪烁/音量协调，不重复实现）。
7. **回归**：既有全部 XCTest 保持全绿（D0-002 后基线 208/208）；主路径（新游戏→174→雨天→事件→水渠 20）回归；新增性能/存档/UX 回归测试。

### 禁止做（Out of scope / 红线，Do Not）

- ❌ 不改 PRD、TDD、`docs/game/project-ledger.md`、本 brief、既有 brief 与既有证据文件。
- ❌ 不自我 `APPROVED`，不修改任务状态，不宣称验收通过。
- ❌ 不实现设置功能本体（机器设置文件/重绑定/缩放等——D0-002 已承接），仅做衔接协调与剩余无障碍项。
- ❌ 不引入正式像素美术/精灵/角色动画/正式音频/混音（属 M4-002 及以后）。
- ❌ 不做发行打包、安装与离线烟雾（M4-002）。
- ❌ 不新增玩法/地图/角色/任务/经济数值；不改 `ContentID.swift` 稳定 ID、`ContentValidator.swift` 校验规则；不删改玩法/经济/社区事务语义（Farm/Inventory/Shipping/Quest/Watershed/Gossip/Relationship/Crafting/Placement/Gather/Sleep）。
- ❌ 性能优化涉及 `Domain/` 的最小改动须逐处声明理由，语义不变。
- ❌ 不 `git commit`、不 `git reset`、不删除工作区未提交内容（M1–D0-002 工作树共存，直接在其上增量工作）。
- ❌ 不复制任何受保护的任务、地图、对白、视觉或音频表达；所有文案原创。

## 4. 可观察验收（全部需证据）

1. **Release 构建**成功（0 编译警告/错误）；性能测量基于 Release 产物。
2. **NFR-001**：1920×1080 10 分钟压力路线，预热后帧时间 P95 ≤16.67 ms、P99 ≤25 ms（Instruments/采样日志原始证据）。
3. **NFR-002**：连续切图 10 次加载 P95 <3 s（原始记录）。
4. **内存**：10 分钟压力路线 RSS 稳定（起始/峰值/结束，无持续增长）。
5. **多槽存档**：3 个手动槽可创建/覆盖/读取互不干扰；睡眠自动写入最近自动槽；自动槽与手动槽分离。
6. **失败安全**：写入失败（故障注入）不破坏最近有效存档（安全替换 + 备份回退，NFR-003）。
7. **UX 修复**：HUD 折叠后地图可见石阶/NPC（截图）；第 3 天目标不再复述第 2 天核实教程（HUD 转储）；水脉显示「20 已达成（含纠正 +2 → 22）」；石阶离开需确认或面对再按（不再踩上即切图）；配方选择切图/读档后保持（HUD 转储）。
8. **无障碍补充**：动态字体/缩放正确适配、半透明叠字减少、色觉外状态图标（截图/转储；与 D0-002 不重复）。
9. **回归**：既有 XCTest 全绿（计数以最新 xcresult 为准）；主路径（新游戏→174→雨天→事件→水渠 20）在优化后仍可走通。
10. 未改动玩法/经济/事务语义；`git diff --check` exit 0；未复制受保护表达。

## 5. 一条烟雾路径（计时主路径）

Release 构建启动 → 新游戏 → 收获 3 株 → 174 结算（回归）→ 打开 HUD 折叠（地图露出石阶）→ 翻种浇 + 采集 + 制作/放置接片 + 事件核实纠正（含目标行状态切换与 22/20 新显示）→ 睡眠（自动存档写入自动槽）→ 手动存 3 个不同槽 → 切图往返 5 次（记录加载时间）→ 读档（配方保持）→ 故障注入写入失败（最近有效存档不破坏）→ 压力路线 10 分钟（帧时间/内存记录）→ 完全退出重启 → 读档状态一致。

## 6. 交付格式（全部放入 `artifacts/integration/m4-001-xcode/`）

1. `cursor-handoff.md`：outcome、变更文件清单（每文件一句）、验收映射表（10 条逐条 → 实现位置 + 证据）、已知问题与风险、下一负责人。
2. Release 构建与回归测试原始证据：
   ```text
   xcodebuild -project macos/CreekSprout/CreekSprout.xcodeproj -scheme CreekSprout \
     -configuration Release -destination 'platform=macOS,arch=arm64' \
     -derivedDataPath /tmp/creeksprout-m4-001 \
     -resultBundlePath artifacts/integration/m4-001-xcode/TestResults-M4-001.xcresult \
     clean test
   ```
   原始日志 + `test-summary.json` + 编译警告计数（Release 构建 0 警告）；记录新旧测试计数。
3. 性能原始数据：`perf/` 下 Instruments trace 或等价采样日志 + 摘要（P95/P99、加载 P95、RSS 起始/峰值/结束）；写明采样方法与计算口径。
4. 运行时证据：HUD 折叠/目标行/水脉显示/石阶/配方截图与 HUD 转储、多槽存档 JSON 清单（槽位隔离）、故障注入日志、回归截图。
5. `git diff --check` 退出 0 的输出；声明未修改文件清单（PRD/TDD/台账/brief/证据）。
6. 性能优化涉及 `Domain/` 最小改动须在 handoff 单独声明（文件、改动、理由、语义不变证明）。

## 7. 回复格式（必须逐项确认，缺项视为未完成）

```
A. 范围确认：本任务 in scope 与红线逐条「确认 / 不确认」；不确认项说明原因。
B. 当前基线核验：运行前 208/208 基线是否已按第 2 节证据路径核实。
C. 交付清单：第 6 节各文件路径 + 状态（已生成/未生成）。
D. 测试结果：xcresult 权威计数（passed/failed/skipped/警告），与第 4.9 条对比。
E. 验收映射：10 条验收逐条 → 证据文件；未达标条目明确声明 REVISE 待办。
F. 风险与红线声明：声明未触碰任何红线；性能优化最小改动清单。
G. 下一负责人：🧱 tech 交付门禁 → 🛡️ quality（Hermes）独立验收。
```

完成后将本文件与 `docs/game/m4-001-brief.md` 一并交给 🧱 tech 与 🛡️ quality 门禁。
